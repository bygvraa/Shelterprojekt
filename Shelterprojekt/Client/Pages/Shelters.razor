@page "/shelters"
@using Shelterprojekt.Client.Shared
@inject HttpClient Http

<h1>Shelters</h1>

<br />

<table class="table">
    <thead>
        <tr>
            <th>Shelter navn</th>
            <th>Placeholder</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var shelter in ShelterList)
        {
<tr>
    <td>@shelter.navn</td>
</tr>}

    </tbody>
</table>
@code { private List<shelterInfo> shopping = new List<shelterInfo>();
            private string newItem;

            private shelterInfo[] ShelterList = Array.Empty<shelterInfo>();



            protected override async Task OnInitializedAsync()
            {
                ShelterList = await Http.GetFromJsonAsync<shelterInfo[]>("shelter");
            }
    }



